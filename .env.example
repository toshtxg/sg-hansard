# Supabase (required unless SKIP_DB=true)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Date range — leave unset to catch up from the latest sitting in DB to today
# RUN_DATE=2025-01-07          # single-date run (YYYY-MM-DD or DD-MM-YYYY)
# START_DATE=2024-01-01
# END_DATE=2024-03-31
# INGEST_LOOKBACK_DAYS=7       # re-ingest window back from the latest sitting (auto mode)
# FETCH_SLEEP_SECS=1           # politeness delay between per-day fetches
# MAX_DAYS_PER_RUN=0           # safety cap; 0 disables

# Local dev
# SKIP_DB=false                # parse only, never write to Supabase
# DEBUG=false                  # verbose logs + per-sitting CSVs
# SAVE_JSON=false              # when DEBUG=true, also dump raw API JSON

# AI summaries (optional)
# AI_ENABLED=false
# AI_PROVIDER=openai
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
# AI_MAX_CHARS=12000
# AI_DRY_RUN=false             # generate but do not persist
# AI_FORCE=false               # re-generate sitting summaries that already exist
