[pytest]
# Put the repo root on sys.path so `import hansard_ingest` works without an
# installed package (mirrors how ingest.py runs from the repo root).
pythonpath = .
testpaths = tests
addopts = -q
